version: '3.12'

services:
  backend:
    build:
      context: ./backend
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - FRONTEND_URL=http://ec2-51-20-132-244.eu-north-1.compute.amazonaws.com:8080
    depends_on:
      - db

  frontend:
    build:
      context: ./frontend
    ports:
      - "8080:80"

  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: reservations
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
